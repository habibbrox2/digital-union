{% extends 'layout.twig' %}

{% block title %}ভূমিকা মুছুন - নিশ্চিতকরণ{% endblock %}
{% block header_title %}{{ header_title|default('ভূমিকা মুছুন') }}{% endblock %}

{% block content %}
{% include 'components/sweetAlert.twig' %}
{% include 'components/rbac_ui.twig' %}
<div class="container-fluid py-4">
    <div class="rbac-header">
        <div>
            <h4><i class="bi bi-trash3 text-danger me-2"></i>ভূমিকা মুছুন</h4>
            <div class="subtitle">এই কাজটি ফিরানো যাবে না</div>
        </div>
        <a href="/roles" class="btn btn-outline-secondary rbac-btn">
            <i class="bi bi-arrow-left me-1"></i> তালিকায় ফিরুন
        </a>
    </div>

    <div class="row justify-content-center">
        <div class="col-12 col-lg-7">
            <div class="rbac-danger-card shadow-sm">
                <div class="alert alert-warning d-flex align-items-center rounded-top m-0" style="border-radius:16px 16px 0 0;">
                    <i class="bi bi-exclamation-triangle-fill fs-4 me-2"></i>
                    <span><strong>সতর্কতা!</strong> এই কাজটি ফিরানো যাবে না।</span>
                </div>

                <div class="p-4">
                    <div class="text-muted small text-uppercase fw-bold mb-2">মোছার জন্য ভূমিকা</div>
                    <ul class="list-group list-group-flush border rounded mb-4">
                        <li class="list-group-item d-flex justify-content-between">
                            <span class="text-muted">আইডি</span>
                            <span class="badge rounded-pill rbac-badge-secondary">{{ role.role_id }}</span>
                        </li>
                        <li class="list-group-item d-flex justify-content-between">
                            <span class="text-muted">নাম</span>
                            <span class="fw-semibold">{{ role.role_name|e }}</span>
                        </li>
                        <li class="list-group-item d-flex justify-content-between">
                            <span class="text-muted">বিবরণ</span>
                            <span>{{ role.description|e }}</span>
                        </li>
                        <li class="list-group-item d-flex justify-content-between">
                            <span class="text-muted">তৈরি</span>
                            <span>{{ role.created_at|date('d-m-Y H:i') }}</span>
                        </li>
                    </ul>

                    <div class="alert alert-light border mb-4">
                        <i class="bi bi-info-circle me-2"></i>
                        মুছে ফেললে এই ভূমিকাটি সব ব্যবহারকারী থেকে সরিয়ে নেওয়া হবে।
                    </div>

                    <form method="post" action="/roles/{{ role.role_id }}/delete">
                        <input type="hidden" name="csrf_token" value="{{ csrf_token }}" />
                        <div class="d-flex gap-2">
                            <button class="btn btn-danger rbac-btn" type="submit">
                                <i class="bi bi-trash3 me-1"></i> হ্যাঁ, মুছুন
                            </button>
                            <a href="/roles" class="btn btn-outline-secondary rbac-btn">
                                <i class="bi bi-x-circle me-1"></i> বাতিল
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
